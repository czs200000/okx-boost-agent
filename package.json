{
  "name": "okx-boost-agent-workflow",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Local-first OKX Boost strategy dashboard with deterministic risk and liquidity controls",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.73.0"
  }
}
